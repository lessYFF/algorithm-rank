/**
 * 基数排序
 */

import Base from '../base/index';

class RadixClass extends Base {
    constructor(props) {
        super(props)
    }

    /**
     * 基数排序实现
     * @param arr
     * @returns {Array}
     */
    radixRank(arr = []) {
        return arr;
    }
}

export default new RadixClass();
