/**
 * 堆排序
 */

import Base from '../base/index';

class HeapClass extends Base {
    constructor(props) {
        super(props)
    }

    /**
     * 堆排序实现
     * @param arr
     * @returns {Array}
     */
    heapRank(arr = []) {
        return arr;
    }

}

export default new HeapClass();
