/**
 * 计数排序
 */

import Base from '../base/index';

class CountClass extends Base {
    constructor(props) {
        super(props)
    }

    /**
     * 计数排序实现
     * @param arr
     * @returns {Array}
     */
    countRank(arr = []) {
        return arr;
    }

}

export default new CountClass();
