/**
 * 桶排序
 */

import Base from '../base/index';

class BucketClass extends Base {
    constructor(props) {
        super(props)
    }

    /**
     * 桶排序实现
     * @param arr
     * @returns {Array}
     */
    bucketRank(arr = []) {
        return arr;
    }

}

export default new BucketClass();
