/**
 * 快速排序
 */

import Base from '../base/index';

class QuickClass extends Base {
    constructor(props) {
        super(props)
    }

    /**
     * 快速排序实现
     * @param arr
     * @returns {Array}
     */
    quickRank(arr = []) {
        return arr;
    }

}

export default new QuickClass();
